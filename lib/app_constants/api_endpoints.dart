class ApiEndpints {
  static const baseUrl = "https://poolit.theclosedoor.com/api/";
  static const sendOtp = "${baseUrl}sendOtpToPhone";
  static const verifyOtp = "${baseUrl}login";
  static const childrenList = "${baseUrl}children";
}
